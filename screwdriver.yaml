
shared:
    description: 'hi!'
    image: ubuntu
    environment:
        GOPATH: /sd/workspace
        
jobs:
    main:
        steps:
            - test-mount: echo faketoken > /sd/token
            - docker: docker run -v /sd/token/:/tmp/token/ node:6 cat /tmp/token
            - docker-ps: docker ps -a
  
