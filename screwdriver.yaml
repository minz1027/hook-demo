
workflow:
    - publish
shared:
    image: golang
    environment:
        GOPATH: /sd/workspace
        
jobs:
    main:
        steps:
            - echo: echo $SD_TOKEN
            - print: echo the main jobs started!
            - check-go-img: go version
    publish:
        steps:
            - echo: echo $SD_TOKEN
