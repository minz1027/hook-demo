
shared:
    image: golang:latest
    environment:
        GOPATH: /sd/workspace
        
jobs:
    no_main:
        requires: [~commit, ~pr, ~sd@241:main]
        steps:
            - print: echo running mai!
    test_new_main:
        requires: [~commit, ~pr, ~sd@241:main]
        steps:
            - other_print: echo running other_main
    publish:
       requires: no_main
       steps:
           - print: echo running publish
    other_publish:
       requires: test_new_main
       steps:
           - print: echo running other_publish
