
shared:
    description: 'hi!@@'
    image: golang:latest
    steps:
      - test1: &test1 echo shared
    environment:
        GOPATH: /sd/workspace
        
jobs:
    main:
        requires: [~commit, ~pr]
        steps:
            - print: echo Starting main
    publish:
       requires: main
       steps:
           - print: echo publishing it
