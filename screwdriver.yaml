
workflow:
    - second
shared:
    image: node:6
jobs:
    main:
        steps:
            - install: npm install
    publish:
        steps:
            - publish: npm run semantic-release
        secrets:
            - GH_TOKEN
            - NPM_TOKEN
